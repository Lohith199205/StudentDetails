import { AddColourDirective } from './add-colour.directive';

describe('AddColourDirective', () => {
  it('should create an instance', () => {
    const directive = new AddColourDirective();
    expect(directive).toBeTruthy();
  });
});
